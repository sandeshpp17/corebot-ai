services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: corebot
      POSTGRES_USER: ${DB_USER:-corebot}
      POSTGRES_PASSWORD: ${DB_PASS:-corebot}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "${DB_USER:-corebot}"]
      interval: 10s
      timeout: 5s
      retries: 10

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 20

  ollama-init:
    image: ollama/ollama
    depends_on:
      ollama:
        condition: service_healthy
    environment:
      OLLAMA_HOST: http://ollama:11434
      OLLAMA_CHAT_MODEL: ${OLLAMA_CHAT_MODEL:-phi3:mini}
    entrypoint:
      - /bin/sh
      - -c
      - |
        ollama show "$OLLAMA_CHAT_MODEL" >/dev/null 2>&1 || ollama pull "$OLLAMA_CHAT_MODEL"
    restart: "no"

  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+psycopg2://${DB_USER:-corebot}:${DB_PASS:-corebot}@postgres:5432/corebot
      OLLAMA_BASE_URL: http://ollama:11434
      OLLAMA_CHAT_MODEL: ${OLLAMA_CHAT_MODEL:-phi3:mini}
      OLLAMA_EMBED_MODEL: ${OLLAMA_EMBED_MODEL:-phi3:mini}
      EMBEDDING_DIM: ${EMBEDDING_DIM:-3072}
      OLLAMA_TIMEOUT_SEC: ${OLLAMA_TIMEOUT_SEC:-120}
      OLLAMA_EMBED_CONCURRENCY: ${OLLAMA_EMBED_CONCURRENCY:-4}
    depends_on:
      postgres:
        condition: service_healthy
      ollama:
        condition: service_healthy
      ollama-init:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/health', timeout=3)",
        ]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
  ollama:
